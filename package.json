{
  "name": "app",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
    "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --outdir=app/assets/builds --loader:.css=text --loader:.scss=text --watch"
  }
  ,
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
